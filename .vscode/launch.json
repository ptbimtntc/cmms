{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "🚀 Start Website (DB + Build + Serve)",
            "type": "node-terminal",
            "request": "launch",
            "command": "docker-compose up -d && npm run build && php artisan serve"
        },
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9003
        }
    ]
}
